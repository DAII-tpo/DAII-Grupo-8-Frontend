import { Stack, Text, Title } from '@mantine/core';

export function LoginPage() {
  return (
    <Stack gap="xs">
      <Title order={1}>Login</Title>
      <Text c="dimmed">
        Pantalla base pendiente de integracion con el modulo centralizado de
        autenticacion.
      </Text>
    </Stack>
  );
}
